# begin VFM rules

# 1. Nonaktifkan Directory Listing (Keamanan)
Options -Indexes

# 2. Blokir eksekusi PHP di folder uploads
<FilesMatch "\.ph(p[3457s]?|t|tml)$">
    # Untuk Apache 2.4+ (mod_authz_core)
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    # Untuk Apache 2.2
    <IfModule !mod_authz_core.c>
        Order deny,allow
        Deny from all
    </IfModule>
</FilesMatch>

# 3. Nonaktifkan CGI
Options -ExecCGI

# 4. Hapus handler PHP (kompatibel mod_php dan PHP-FPM)
<IfModule mod_php.c>
    <FilesMatch "\.php$">
        php_flag engine off
    </FilesMatch>
</IfModule>
<IfModule mod_php7.c>
    <FilesMatch "\.php$">
        php_flag engine off
    </FilesMatch>
</IfModule>

# end VFM rules